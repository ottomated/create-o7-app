datasource db {
  provider = "sqlite"
}

generator kysely {
  provider = "prisma-kysely"

  output   = "../src/lib/server/db"
  fileName = "schema.d.ts"
}

model Example {
  id   Int    @id @default(autoincrement())
  name String
}
